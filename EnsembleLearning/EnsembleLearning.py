from DecisionTree.ID3 import ID3

def adaboost(T = 500):

    # initialize weights 

    for i in range(T):
        # Train a weak learner
        
        # Update the alpha

        # Update the weights
